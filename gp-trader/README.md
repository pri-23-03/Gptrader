# GPTrader (Phase 1 — Local Emulation)

Quickstart:
1) python3 -m venv .venv && source .venv/bin/activate
2) pip install -U pip && pip install -e . && pip install pytest pytest-cov black isort ruff mypy
3) make all   # lint/type/test

![CI](https://github.com/pri-23-03/Gptrader/actions/workflows/ci.yml/badge.svg?branch=main)
